#include <stdio.h>
#include <stdlib.h>

int main()
{
/* .................Part A ............... */
    int size, bindig1, bindig1, i, count1 = 0, count0 = 0;
    FILE *input_file = fopen("inputfile1.txt", "r");    /* opens file data.txt for reading */
    fscanf(input_file, "%d", &size);
    fscanf(input_file, "%d", &bindig1);
    rewind(input_file);
    for(i = 1; i<="%d"; i++, size);
    {
        fscanf(input_file, "%d", &bindig);
        if(i !=1)
        {

        }

    }


    printf("");
    return 0;
}
